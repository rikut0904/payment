# ================================================================
# 環境変数設定ファイル
# ================================================================
# このファイルをコピーして .env を作成し、実際の値を設定してください
# 詳細な設定手順: doc/getting-started.md
# 環境変数リファレンス: doc/environment-setup.md
# ================================================================

# Firebase クライアント側設定
# Firebase Console → プロジェクト設定 → 全般 → マイアプリから取得
FIREBASE_API_KEY=your_project_secret_id
FIREBASE_AUTH_DOMAIN=your_project_secret_id
FIREBASE_PROJECT_ID=your_project_secret_id
FIREBASE_STORAGE_BUCKET=your_project_secret_id
FIREBASE_MESSAGING_SENDER_ID=your_project_secret_id
FIREBASE_APP_ID=your_project_secret_id
FIREBASE_MEASUREMENT_ID=your_project_secret_id

# Firebase サーバー側設定（Admin SDK）
# Firebase Console → プロジェクト設定 → サービスアカウント → 新しい秘密鍵を生成
FIREBASE_CLIENT_EMAIL=service-account@example.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...YOUR KEY...\n-----END PRIVATE KEY-----\n"
# ログインセッション設定
# SESSION_SECRET: セッションCookieの署名に使用（32文字以上のランダム文字列推奨）
# 生成方法: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=change_me
# SESSION_TIME_MS: セッションの有効期間（ミリ秒）
# 推奨: 600000 (10分) ～ 3600000 (1時間)
SESSION_TIME_MS=600000
# おすすめ表示期間設定
# 「みんなのおすすめ一覧」で表示する投稿の期間を制御
# 優先順位: LIKE_VISIBLE_MINUTES > LIKE_VISIBLE_HOURS > LIKE_VISIBLE_DAYS
# 例: LIKE_VISIBLE_DAYS=7 だと過去7日間の投稿のみ表示
LIKE_VISIBLE_DAYS=7
# LIKE_VISIBLE_HOURS=168
# LIKE_VISIBLE_MINUTES=10080
# 外部APIタイムアウト設定
# 為替レートAPIなどの外部API呼び出し時のタイムアウト（ミリ秒）
# 推奨: 5000 ～ 10000
EXTERNAL_API_TIMEOUT_MS=8000